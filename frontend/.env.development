# 개발: 비워 두기 → fetch 가 /api, /docs, /openapi.json 을 같은 출처(5173)로 보내고
# vite.config.ts 가 http://127.0.0.1:8000 (uvicorn) 으로 프록시합니다.
# 백엔드 먼저: 루트에서 npm run dev 또는 cd backend && python -m uvicorn learning_analysis.main:app --reload --host 127.0.0.1 --port 8000
VITE_API_BASE=
